import React from 'react';
import {styles} from './footerSyles';
import { Container, Header, Title, Content, Footer, FooterTab, Button, Left, Right, Body, Icon, Text } from 'native-base';

export const Foot = ()=>{
    return(
    <Footer>
        <Left>
            <Title>Footer</Title>
        </Left>
    </Footer>
    )
}