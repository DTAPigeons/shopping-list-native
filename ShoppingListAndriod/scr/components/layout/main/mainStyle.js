import {
    StyleSheet,
  } from 'react-native';

  export const styles = StyleSheet.create({
      contaner:{
        alignItems: 'stretch',
        flex: 4, 
        flexDirection: 'column'
      }
  })